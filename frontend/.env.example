# Backend API URL (Railway deployment in prod, localhost in dev)
VITE_API_URL=http://localhost:8000

# Dev-only: real initData string copied from Telegram bot console for local testing
# Open Mini App in Telegram → console.log(WebApp.initData) → paste here
VITE_TEST_INIT_DATA=
